{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "Todo app server",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "jest --ci --reporters=default --reporters=jest-junit"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "jest-junit": "^17.0.0"
  }
}
